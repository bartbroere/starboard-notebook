{
  "image": "mcr.microsoft.com/devcontainers/typescript-node:0-18",
  "postCreateCommand": "bash -i -c 'npm install -g lerna && yarn run bootstrap && yarn install && yarn build'",
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint"]
    }
  },
  "forwardPorts": [9001]
}
